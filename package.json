{
  "name": "admin11467-net",
  "private": true,
  "scripts": {
    "dev": "hugo server --source site",
    "build": "hugo --source site --gc --minify && pagefind --site public",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "hugo-extended": "^0.139.0",
    "pagefind": "^1.3.0",
    "wrangler": "^4.0.0"
  }
}
